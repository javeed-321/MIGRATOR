---
title: Get Companies by Extended Field Values
---

> ## Documentation Index
> Fetch the complete documentation index at: https://docs.capillarytech.com/llms.txt
> Use this file to discover all available pages before exploring further.

## Get Companies by Extended Field Values

Fetches companies using the first few values of an extended field value provided.

## OpenAPI definition

```json
{
  "openapi": "3.1.0",
  "info": {
    "title": "v2",
    "version": "1.0"
  },
  "servers": [
    {
      "url": "https://{host}/v2",
      "variables": {
        "host": {
          "default": "host"
        }
      }
    }
  ],
  "components": {
    "securitySchemes": {
      "sec0": {
        "type": "http",
        "scheme": "basic"
      }
    }
  },
  "security": [
    {
      "sec0": []
    }
  ],
  "paths": {
    "/companies/extendedFieldSearch": {
      "get": {
        "summary": "Get Companies by Extended Field Values",
        "description": "Fetches companies using the first few values of an extended field value provided.",
        "operationId": "get-companies-by-extended-field-values",
        "parameters": [
          {
            "name": "extendedFieldName",
            "in": "query",
            "description": "Name of the extended field by which companies need to be fetched.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "q",
            "in": "query",
            "description": "The first few characters of the extended field value. Pass at least 3 characters. For example, `?extendedFieldName=city&q=ban` fetches all the companies that has the city name starting with Ban.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Number of results to relative.",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "status",
            "in": "query",
            "description": "Filter companies by status.",
            "schema": {
              "type": "string",
              "enum": [
                "ACTIVE",
                "INACTIVE"
              ],
              "default": "ACTIVE"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Always sort results by created date.",
            "schema": {
              "type": "string",
              "enum": [
                "createdDate"
              ]
            }
          },
          {
            "name": "sortOrder",
            "in": "query",
            "description": "Sort the results in ascending or descending order of the chosen `soryBy`.",
            "schema": {
              "type": "string",
              "enum": [
                "asc",
                "desc"
              ]
            }
          },
          {
            "name": "offset",
            "in": "query",
            "description": "Number of rows to be ignored from the top (pagination).",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "400": {
            "description": "400",
            "content": {
              "application/json": {
                "examples": {
                  "Result": {
                    "value": "{}"
                  }
                },
                "schema": {
                  "type": "object",
                  "properties": {}
                }
              }
            }
          }
        },
        "deprecated": false
      }
    }
  },
  "x-readme": {
    "headers": [],
    "explorer-enabled": true,
    "proxy-enabled": true
  },
  "x-readme-fauxas": true
}
```
